version: "3.8"

services:
  slovnicek:
    build: .
    ports:
      - "8081:8081"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/slovnicek.db
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped